services:
  backend:
    build: .
    ports:
      - "5000:5000"
    image: tnanh1407xyz/pixelmart-backend-version-01:latest
    env_file:
      - .env
    restart: unless-stopped
         # bị crash sẽ tự chạy lại
volumes:
  mongo_data:
